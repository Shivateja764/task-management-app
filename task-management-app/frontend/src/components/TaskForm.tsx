'use client'; import {FormEvent,useState} from 'react'; import {api} from '../lib/api'; import {Task} from '../types';
export function TaskForm({task,onDone,onClose}:{task?:Task,onDone:()=>void,onClose:()=>void}){const [title,setTitle]=useState(task?.title||''),[description,setDescription]=useState(task?.description||''),[status,setStatus]=useState(task?.status||'PENDING'),[priority,setPriority]=useState(task?.priority||'MEDIUM'),[dueDate,setDueDate]=useState(task?.dueDate?task.dueDate.slice(0,16):''),[location,setLocation]=useState(task?.location||''),[files,setFiles]=useState<FileList|null>(null),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const f=new FormData();f.append('title',title);f.append('description',description);f.append('status',status);f.append('priority',priority);if(dueDate)f.append('dueDate',new Date(dueDate).toISOString());if(location)f.append('location',location);if(files)Array.from(files).forEach(x=>f.append('files',x));try{if(task)await api.put(`/tasks/${task.id}`,f);else await api.post('/tasks',f);onDone()}catch(e:any){setError(e.response?.data?.message||'Could not save task')}finally{setLoading(false)}}return <div className="modal"><form className="card grid" onSubmit={submit}><div className="row" style={{justifyContent:'space-between'}}><h2>{task?'Edit task':'New task'}</h2><button type="button" className="btn" onClick={onClose}>✕</button></div><label><span className="label">Title</span><input className="input" value={title} onChange={e=>setTitle(e.target.value)} required/></label><label><span className="label">Description</span><textarea className="input" rows={4} value={description} onChange={e=>setDescription(e.target.value)}/></label><div className="grid grid2"><label><span className="label">Status</span><select className="input" value={status} onChange={e=>setStatus(e.target.value as any)}><option>PENDING</option><option>IN_PROGRESS</option><option>DONE</option></select></label><label><span className="label">Priority</span><select className="input" value={priority} onChange={e=>setPriority(e.target.value as any)}><option>LOW</option><option>MEDIUM</option><option>HIGH</option></select></label></div><div className="grid grid2"><label><span className="label">Due date</span><input className="input" type="datetime-local" value={dueDate} onChange={e=>setDueDate(e.target.value)}/></label><label><span className="label">Location / city</span><input className="input" placeholder="Hyderabad" value={location} onChange={e=>setLocation(e.target.value)}/></label></div><label><span className="label">Attachments (up to 5, 10MB each)</span><input className="input" type="file" multiple onChange={e=>setFiles(e.target.files)}/></label>{error&&<div style={{color:'#b91c1c'}}>{Array.isArray(error)?error.join(', '):error}</div>}<div className="row" style={{justifyContent:'flex-end'}}><button type="button" className="btn" onClick={onClose}>Cancel</button><button className="btn primary" disabled={loading}>{loading?'Saving...':'Save task'}</button></div></form></div>}
