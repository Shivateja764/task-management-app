'use client'; import {Task} from '../types'; export function TaskCard({task,onEdit,onDelete}:{task:Task;onEdit:()=>void;onDelete:()=>void}){return <article className="card grid"><div className="row" style={{justifyContent:'space-between',alignItems:'flex-start'}}><div><h3 style={{margin:'0 0 6px'}}>{task.title}</h3><div className="muted" style={{fontSize:13}}>{task.description||'No description'}</div></div><span className="pill" style={{background:task.status==='DONE'?'#dcfce7':task.status==='IN_PROGRESS'?'#dbeafe':'#fef3c7'}}>{task.status}</span></div><div className="row" style={{flexWrap:'wrap'}}><span className="pill" style={{background:task.priority==='HIGH'?'#fee2e2':task.priority==='LOW'?'#e0f2fe':'#f3f4f6'}}>{task.priority}</span>{task.location&&<span className="pill" style={{background:'#f3f4f6'}}>📍 {task.location}</span>}{task.dueDate&&<span className="pill" style={{background:'#f3f4f6'}}>🗓 {new Date(task.dueDate).toLocaleString()}</span>}{task.weather&&<span className="pill" style={{background:'#e0f2fe'}}>☁️ {task.weather.temp}°C · {task.weather.description}</span>}</div>{task.attachments.length>0&&<div><b>Attachments</b><div className="grid" style={{marginTop:6}}>{task.attachments.map(a=><a key={a.id} href={a.url} target="_blank" rel="noreferrer">📎 {a.fileName||'View attachment'}</a>)}</div></div>}<div className="row" style={{justifyContent:'flex-end'}}><button className="btn" onClick={onEdit}>Edit</button><button className="btn danger" onClick={onDelete}>Delete</button></div></article>}
